# # Here you can define credentials for different data sets and environment.
# #

db_creds:
    con: postgresql://${KEDRO_DB_USERNAME}:${KEDRO_DB_PASSWORD}@${KEDRO_DB_HOST}:${KEDRO_DB_PORT|5432}/${KEDRO_DB_DATABASE}
